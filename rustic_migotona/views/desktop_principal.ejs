<!DOCTYPE html>
<html>
    <head>
        <script src="https://kit.fontawesome.com/68bc1fc849.js" crossorigin="anonymous"></script>
        <link rel="stylesheet"  href="css/desktop.css">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
    </head>
    <body>
        <%- include('./partials/header.ejs') %> 
        <main class="main_container">
            <div class="main_container_izq">
                <h1 style="color: white" id="theh1"><%= secret %></h1>
                
                <h1 style="color: white;"><p style="font-size: 3vw;">Categorias</p></h1>
            <div class="main_container_izq_2">
            <button class="botones" id="tabletop"><p style="font-size: 2vw;">Juegos de Mesa</p></button><!--boton juegos de mesa-->
            <button class="botones" id="tcg"><p style="font-size: 2vw;">TCG</p></button><!--boton tcg-->
            <button class="botones" id="books"><p style="font-size: 2vw;">Libros de Terror</p></button><!--boton libros de terror-->
            <button class="botones" id="manga"><p style="font-size: 2vw;">Manga</p></button><!--boton manga-->
            </div>
            </div>
            <div class="main_container_der">
                <h1 style="color: white;"><%= productos[0].descripcion %></h1><!--display categoria del producto como ejemplo juegos de mesa-->
            <div class="main_container_der_2">
                <form action="/detail_product" method="get" >

                </form>

                <% function iterator(indexmin, indexmax, array) { %>
                    <% array2 = []; %>
                    <% if (array.length < indexmax) { %>
                      <% indexmax2 = array.length; %>
                      <% for (let i = indexmin; i < indexmax2; i++) { %>
                        <% array2.push(array[i]); %>
                      <% } %>
                    <% } else { %>
                      <% for (let i = indexmin; i < indexmax; i++) { %>
                        <% array2.push(array[i]); %>
                      <% } %>
                    <% } %>
                  
                    <% return array2; %>
                 <% } %>

                 <% function writer(){console.log('works!')} %>
                 <% var min = 0 %>
                 <% var max = 2 %>
                 
                 
                <% let arreglo = iterator(min,max,productos) %>
                <% arreglo.forEach(element => { %>
                    <% if(element.id != 0){ %>
                    <div class="main_container_der_3"><!--Listado de productos , un producto en cada div-->
                        
                    </div>
                    <% } %>
                    
                <% }) %>
                
                
                

                

                

                
                
            </div>
            <button  style="height: 50px; width: 50px;" id="redux_up" >+</button>

        </main>
        <%- include('./partials/footer.ejs') %> 
        
        
    </body>
    <script src="./scrpits_js/desktop_products.js"></script>
    <script>
        window.addEventListener("load", loader);
        /////// Array of elements
        let products_array = []
            let string_array = ''
            let newarray = []
            let products = []
            products_array = "<%= JSON.stringify(productos) %>"
            string_array = products_array.replace(/&#34;/ig,'"');
            const middlevar = JSON.stringify(string_array) 
            products =JSON.parse(middlevar)
        
        
        function loader(){
            
            stringgg = String(products)
            productos = JSON.parse(stringgg)
            

            //parent container
            const div= document.getElementsByClassName("main_container_der_2")
            const form = document.getElementsByTagName("form")
            
            //ptag
            const p = document.createElement("p")
            p.id = productos[0].id
            p.style.color = "white"
            p.textContent = productos[1].nombre
            div[0].appendChild(p)
            
              //button
              const button = document.createElement("button")
              button.type = "submit"
              button.class = "bottones_der_2"
              button.style = `background-image: url(${productos[1].imagen})`
              form[0].appendChild(button)
              console.log(productos[1].imagen)
              


            


        }
        let button_ev = document.getElementById("redux_up")
            button_ev.addEventListener("click",min_max_up)
        
        function paginator(min,max,step){ 
            let div = document.createElement("div")
            div.className("items_price")
            
            
            
        } 
        
        


        // function iterator(indexmin, indexmax, array) { 
        //              array2 = []; 
        //              if (array.length < indexmax) { 
        //                indexmax2 = array.length; 
        //                for (let i = indexmin; i < indexmax2; i++) { 
        //                  array2.push(array[i]); 
        //                } 
        //              } else { 
        //                for (let i = indexmin; i < indexmax; i++) {
        //                  array2.push(array[i]);
        //                } 
        //              } 
                  
        //              return array2; 
        //           } 

    </script>

    <!-- <script src="./scrpits_js/desktop_redux.ts"></script> -->
    
 
</html>